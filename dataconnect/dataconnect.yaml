specVersion: "v1beta"
serviceId: "printi"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "printi-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
